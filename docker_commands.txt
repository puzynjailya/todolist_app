#Network creation:
docker network create todolist_app_net

# Postgres container creation:
docker run --name todolist_postgres -e POSTGRES_USER=tdl_2022 -e POSTGRES_PASSWORD=G3OWuzMdhY7SHlsgyeChKbU_oP26yItgptzapVoh9gk
-e POSTGRES_DB=tdl_db --network=todolist_app_net --net
work-alias pg -d postgres

# App container creation:
docker build -t todolist_app .
docker run -d --network=todolist_app_net -p 8000:8000 --name=todolist_app todolist_app

# Confirm migrations:
docker exec -it todolist_app /bin/bash
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser

# Container with front code
# Required to build container from docker's parent folder
docker build -f docker\Dockerfile -t tdl_front .
